# Person profiles

This directory contains curated human-readable person profiles. Profiles may be
maintained manually while the schema is stabilising. Generated views must not
be introduced until the structured model has been tested against several real
sources.

Use lowercase kebab-case filenames and include the stable person ID in the
front matter. Begin from `templates/person-profile.md`.
