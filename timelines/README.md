# Timelines

This directory is reserved for source-linked derived timelines. Do not create a
timeline from `CURRENT_STATE.md` alone. Timeline generation begins only after
the structured schema has passed the first-source checkpoint.

Every displayed event must link to its event ID and source IDs, preserve date
precision and expose conflicts rather than selecting an unsupported date.
